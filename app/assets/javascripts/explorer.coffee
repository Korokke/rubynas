@show_upload_form = ->
  $('#upload-field').show()
  $('#upload-btn').hide()
@hide_upload_form = ->
  $('#upload-field').hide()
  $('#upload-btn').show()

@show_newfolder_form = ->
  $('#newfolder-field').show()
  $('#newfolder-btn').hide()
@hide_newfolder_form = ->
  $('#newfolder-field').hide()
  $('#newfolder-btn').show()
